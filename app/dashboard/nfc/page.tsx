import { DashboardShell, Heading } from '@/components/dashboard-shell'
import { nfcCards } from '@/lib/mock-data'
export default function NfcPage() { return <DashboardShell><Heading eyebrow="NFC cards" title="Your feedback touchpoints" description="Manage every tap point across your business." action="Create card"/><div className="grid gap-4 md:grid-cols-2">{nfcCards.map((card) => <article key={card.id} className="rounded-3xl border border-border bg-card p-6"><div className="flex items-center justify-between"><div><p className="text-sm text-muted-foreground">{card.location}</p><h2 className="mt-1 text-xl font-semibold">Urban Brew counter</h2></div><span className="rounded-full bg-[#dcefe3] px-3 py-1 text-xs font-semibold text-[#2e7654]">Active</span></div><p className="mt-8 rounded-2xl bg-secondary p-4 font-mono text-sm">{card.nfcId}</p></article>)}</div></DashboardShell> }
