'use client'
import { useState } from 'react'
import Link from 'next/link'
import { ArrowRight } from 'lucide-react'
import { DashboardShell, Heading } from '@/components/dashboard-shell'
import { getReviewDetailRoute } from '@/lib/routes'
const reviews=[['review-001','Maya R.','MR','5','The cardamom latte was incredible. Such a beautiful little space for a slow morning.','NFC card','12 min ago'],['review-002','James K.','JK','5','Friendly service, great playlist, and the best cortado I have had in months.','QR code','46 min ago'],['review-003','Ari L.','AL','4','Loved the coffee and the staff. It got busy around lunch, but still worth the visit.','NFC card','2 hrs ago'],['review-004','Nora P.','NP','5','A genuinely lovely coffee shop. The little details make all the difference.','QR code','Yesterday']]
export default function Reviews(){const [filter,setFilter]=useState('All reviews');return <DashboardShell><Heading eyebrow="Feedback inbox" title="Reviews" description="Every note from your customers, in one thoughtful place." action="Export CSV"/><div className="mb-5 flex flex-wrap gap-2">{['All reviews','5 stars','Needs reply','This month'].map(item=><button key={item} onClick={()=>setFilter(item)} className={`rounded-full px-4 py-2 text-sm font-medium ${filter===item?'bg-[#183b34] text-[#f8f5ed]':'border border-border bg-card text-muted-foreground'}`}>{item}</button>)}</div><div className="overflow-hidden rounded-3xl border border-border bg-card">{reviews.map(([id,name,initials,score,text,source,time])=><Link href={getReviewDetailRoute(id)} key={id} className="grid gap-3 border-b border-border px-5 py-5 transition hover:bg-secondary last:border-0 md:grid-cols-[1.2fr_2fr_.8fr_.8fr] md:items-center md:gap-5 md:px-6"><div className="flex items-center gap-3"><div className="grid size-9 place-items-center rounded-full bg-[#f4c7a1] text-xs font-semibold text-[#183b34]">{initials}</div><div><p className="text-sm font-medium">{name}</p><p className="text-xs text-[#dc7251]">{'★'.repeat(Number(score))}</p></div></div><p className="text-sm leading-6 text-muted-foreground">{text}</p><span className="w-fit rounded-full bg-secondary px-2.5 py-1 text-xs text-muted-foreground">{source}</span><span className="flex items-center justify-between text-xs text-muted-foreground">{time}<ArrowRight size={15}/></span></Link>)}</div></DashboardShell>}
