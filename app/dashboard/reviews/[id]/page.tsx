import Link from 'next/link'
import { notFound } from 'next/navigation'
import { ArrowLeft, Star } from 'lucide-react'
import { DashboardShell } from '@/components/dashboard-shell'
import { getReviewById } from '@/lib/mock-data'
import { ROUTES } from '@/lib/routes'
export default async function ReviewDetailPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const review = getReviewById(id); if (!review) return <DashboardShell><div className="mx-auto max-w-xl rounded-3xl border border-border bg-card p-10 text-center"><p className="text-sm font-semibold uppercase tracking-[0.16em] text-[#dc7251]">Review unavailable</p><h1 className="mt-3 text-3xl font-semibold">We couldn&apos;t find that review.</h1><Link href={ROUTES.reviews} className="mt-8 inline-flex rounded-full bg-[#183b34] px-5 py-3 text-sm font-semibold text-[#fff8ed]">Back to reviews</Link></div></DashboardShell>; return <DashboardShell><Link href={ROUTES.reviews} className="inline-flex items-center gap-2 text-sm text-muted-foreground"><ArrowLeft size={16}/>Back to reviews</Link><article className="mt-8 max-w-3xl rounded-3xl border border-border bg-card p-6 md:p-10"><div className="flex items-center justify-between"><div><p className="text-sm text-muted-foreground">{review.createdAt}</p><h1 className="mt-2 text-3xl font-semibold">{review.author}</h1></div><div className="flex gap-1 text-[#e4a44c]">{Array.from({ length: review.rating }).map((_, index) => <Star key={index} fill="currentColor" size={18}/>)}</div></div><p className="mt-10 text-xl leading-8">&quot;{review.text}&quot;</p><div className="mt-8 rounded-2xl bg-secondary p-5"><p className="text-sm font-semibold">Milo&apos;s summary</p><p className="mt-2 text-sm leading-6 text-muted-foreground">This {review.sentiment} review gives the team a clear signal about the customer experience.</p></div></article></DashboardShell> }
