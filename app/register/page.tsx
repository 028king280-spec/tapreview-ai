'use client'
import { FormEvent, useState } from 'react'
import Link from 'next/link'
import { useRouter } from 'next/navigation'
import { ArrowLeft, ArrowRight, ScanLine } from 'lucide-react'
import { ROUTES } from '@/lib/routes'
export default function RegisterPage() { const router = useRouter(); const [name, setName] = useState(''); const submit = (event: FormEvent) => { event.preventDefault(); if (name.trim()) { sessionStorage.setItem('tapreview-business-name', name.trim()); router.push(ROUTES.onboarding) } }; return <main className="grid min-h-screen place-items-center bg-[#fff8ed] px-5 py-10 text-[#183b34]"><div className="w-full max-w-md"><Link href="/" className="mb-12 inline-flex items-center gap-2 text-sm text-[#71827b]"><ArrowLeft size={16}/>Back to home</Link><div className="mb-8 flex items-center gap-2 font-semibold"><span className="grid size-9 place-items-center rounded-xl bg-[#183b34] text-[#fff8ed]"><ScanLine size={18}/></span>tap<span className="text-[#dc7251]">review</span></div><h1 className="text-4xl font-semibold tracking-tight">Bring every customer voice closer.</h1><p className="mt-3 leading-6 text-[#71827b]">Set up your feedback experience in a few simple steps.</p><form onSubmit={submit} className="mt-8 flex flex-col gap-4 rounded-3xl border border-[#eadfd2] bg-white/80 p-5 shadow-sm"><label className="text-sm font-semibold" htmlFor="business">Business name<input id="business" required value={name} onChange={(event) => setName(event.target.value)} placeholder="Urban Brew Cafe" className="mt-2 w-full rounded-2xl border border-[#eadfd2] bg-white px-4 py-3 outline-none focus:border-[#183b34]"/></label><button className="inline-flex items-center justify-center gap-2 rounded-full bg-[#183b34] px-5 py-3 font-semibold text-[#fff8ed]">Continue<ArrowRight size={17}/></button></form></div></main> }
