'use client'
import { useState } from 'react'
import { useRouter } from 'next/navigation'
import { ArrowLeft, ArrowRight, Check, Sparkles } from 'lucide-react'
import { ROUTES } from '@/lib/routes'
const steps = ['Business details','Branding','AI character','AI greeting','Voice','Review links','NFC card','Test experience']
export default function OnboardingPage() { const router = useRouter(); const [step, setStep] = useState(0); const next = () => step === steps.length - 1 ? router.push(ROUTES.dashboard) : setStep((value) => value + 1); return <main className="min-h-screen bg-[#fff8ed] px-5 py-8 text-[#183b34] md:px-10"><div className="mx-auto max-w-4xl"><div className="flex items-center justify-between"><div className="flex items-center gap-2 font-semibold"><span className="grid size-9 place-items-center rounded-xl bg-[#183b34] text-[#fff8ed]">TR</span>tap<span className="text-[#dc7251]">review</span></div><button onClick={() => router.push(ROUTES.dashboard)} className="text-sm text-[#71827b]">Skip setup</button></div><div className="mt-12 grid gap-8 md:grid-cols-[220px_1fr]"><aside className="flex gap-2 overflow-x-auto md:flex-col">{steps.map((label, index) => <button key={label} onClick={() => setStep(index)} className={`whitespace-nowrap rounded-xl px-3 py-2 text-left text-sm ${index === step ? 'bg-[#183b34] font-semibold text-[#fff8ed]' : 'text-[#71827b] hover:bg-white'}`}>{index < step ? <Check className="mr-2 inline" size={15}/> : `${index + 1}. `}{label}</button>)}</aside><section className="rounded-[2rem] border border-[#eadfd2] bg-white/85 p-6 shadow-sm md:p-10"><div className="mb-12 flex size-14 items-center justify-center rounded-2xl bg-[#f3c5a0] text-[#183b34]"><Sparkles size={24}/></div><p className="text-sm font-semibold uppercase tracking-[0.16em] text-[#dc7251]">Step {step + 1} of {steps.length}</p><h1 className="mt-3 text-3xl font-semibold tracking-tight">{steps[step]}</h1><p className="mt-3 max-w-lg leading-7 text-[#71827b]">Let&apos;s shape a thoughtful feedback experience for your customers. You can change these choices later from your dashboard.</p><div className="mt-10 rounded-2xl bg-[#f7f2eb] p-5 text-sm text-[#71827b]">Your progress is saved for this demo workspace. Choose Next to continue.</div><div className="mt-10 flex justify-between"><button disabled={step === 0} onClick={() => setStep((value) => value - 1)} className="inline-flex items-center gap-2 rounded-full border border-[#eadfd2] px-4 py-2.5 text-sm disabled:opacity-40"><ArrowLeft size={16}/>Back</button><button onClick={next} className="inline-flex items-center gap-2 rounded-full bg-[#183b34] px-5 py-2.5 text-sm font-semibold text-[#fff8ed]">{step === steps.length - 1 ? 'Finish setup' : 'Next'}<ArrowRight size={16}/></button></div></section></div></div></main> }
